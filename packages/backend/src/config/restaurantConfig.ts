export const restaurantConfig = {
    name: process.env.RESTAURANT_NAME || 'Mi Restaurante',
    description: process.env.RESTAURANT_DESCRIPTION || 'Descripción del restaurante',
    address: process.env.RESTAURANT_ADDRESS || 'Dirección del restaurante',
    phone: process.env.RESTAURANT_PHONE || 'Teléfono del restaurante',
    // Puedes agregar más configuraciones según sea necesario
  };
  
  